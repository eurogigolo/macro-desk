# Job Brief: Neon Pulse Button (Reverted)

## Mission
The Pulse button implementation requested earlier was reverted per leadership request. This brief documents the rollback decision and ensures the orchestrator artifacts remain available for traceability.

## Scope Notes
- The `karkinos-orchestrator/pulse-button` folder now retains only rollout metadata; no code changes remain on the page.
- The revert addresses the request to remove the previous commit while keeping our documentation trail intact.

## Constraints
- Do not reintroduce UI changes until a new directive arrives.
- Keep the artifacts reflective of the reversal (decision log, risk register, validation ledger, handoff log) so future reviewers understand the lifecycle.
