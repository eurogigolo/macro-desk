# Decision Log

- **2026-05-05:** Chose to implement the Pulse button as a fixed-position overlay with a transparent background wrapper so the existing masthead and cards are unaffected while keeping the button centered in the viewport.
- **2026-05-05:** Added transient text swap via inline script so each click shows "Epic Win!" before returning to "Pulse," keeping the button responsive for multiple clicks.
